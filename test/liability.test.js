const assert = require('assert');
    const liability = require('../index');
    
    describe('liability Testing', () => {
      it('should verify liability does exist', () => {
        // Sample test
        assert.ok(liability, 'liability should be defined');
      });
    });