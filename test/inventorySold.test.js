const assert = require('assert');
    const inventorySold = require('../index');
    
    describe('inventorySold Testing', () => {
      it('should verify inventorySold does exist', () => {
        // Sample test
        assert.ok(inventorySold, 'inventorySold should be defined');
      });
    });