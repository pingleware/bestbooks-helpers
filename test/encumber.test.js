const assert = require('assert');
    const encumber = require('../index');
    
    describe('encumber Testing', () => {
      it('should verify encumber does exist', () => {
        // Sample test
        assert.ok(encumber, 'encumber should be defined');
      });
    });