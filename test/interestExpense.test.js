const assert = require('assert');
    const interestExpense = require('../index');
    
    describe('interestExpense Testing', () => {
      it('should verify interestExpense does exist', () => {
        // Sample test
        assert.ok(interestExpense, 'interestExpense should be defined');
      });
    });