const assert = require('assert');
    const deferredExpense = require('../index');
    
    describe('deferredExpense Testing', () => {
      it('should verify deferredExpense does exist', () => {
        // Sample test
        assert.ok(deferredExpense, 'deferredExpense should be defined');
      });
    });