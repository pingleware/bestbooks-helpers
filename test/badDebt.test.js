const assert = require('assert');
    const badDebt = require('../index');
    
    describe('badDebt Testing', () => {
      it('should verify badDebt does exist', () => {
        // Sample test
        assert.ok(badDebt, 'badDebt should be defined');
      });
    });