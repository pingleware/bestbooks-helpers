const assert = require('assert');
    const equity = require('../index');
    
    describe('equity Testing', () => {
      it('should verify equity does exist', () => {
        // Sample test
        assert.ok(equity, 'equity should be defined');
      });
    });